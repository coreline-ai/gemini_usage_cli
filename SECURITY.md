# Security Policy

## 보안 정책

### 지원 버전

| 버전 | 지원 상태 |
|------|----------|
| 0.1.x | ✅ 지원 |

### 취약점 신고

보안 취약점을 발견하셨다면 아래 방법으로 신고해 주세요:

1. **비공개 신고**: security@example.com으로 이메일 전송
2. **GitHub Security Advisory**: [보안 탭](https://github.com/yourusername/gemini-usage-cli/security/advisories)에서 비공개 취약점 리포트 생성

**응답 시간**: 영업일 기준 48시간 이내 응답

---

## 보안 아키텍처

### 데이터 저장

| 항목 | 위치 | 설명 |
|------|------|------|
| 사용량 로그 | `~/.gemini/usage.jsonl` | 로컬 파일만 사용, 외부 전송 없음 |
| 설정 | `~/.config/gemini-usage-cli/` | `conf` 라이브러리로 관리 |
| 규칙 파일 | `.antigravity/rules.md` | 프로젝트 또는 전역 경로 |

### 네트워크 통신

- ⚠️ **현재 버전은 네트워크 통신을 사용하지 않습니다**
- 모든 데이터는 로컬 파일 시스템에서만 처리됩니다
- 향후 API 통합 시 HTTPS만 사용 예정

### 인증 및 권한

| 기능 | 현재 상태 | 설명 |
|------|----------|------|
| API 토큰 | 예시 구현 | 프로덕션 미사용 |
| 토큰 마스킹 | ✅ 구현됨 | 앞 8자리만 표시 |
| 파일 권한 | 사용자 권한 | 로컬 사용자만 접근 가능 |

---

## 보안 모범 사례

### 사용자 권장사항

1. **로그 파일 보호**
   ```bash
   chmod 600 ~/.gemini/usage.jsonl
   ```

2. **설정 파일 권한 확인**
   ```bash
   ls -la ~/.config/gemini-usage-cli/
   ```

3. **민감 정보 제외**
   - API 키를 로그 파일에 기록하지 않음
   - 토큰은 항상 마스킹 처리

### 개발자 권장사항

1. **의존성 감사**
   ```bash
   npm audit
   ```

2. **정기 업데이트**
   ```bash
   npm outdated
   npm update
   ```

3. **코드 리뷰**
   - 모든 PR은 보안 검토 필수
   - 민감 정보 하드코딩 금지

---

## 의존성 보안

### 현재 상태

```bash
$ npm audit
found 0 vulnerabilities
```

### 주요 의존성

| 패키지 | 용도 | 보안 고려사항 |
|--------|------|--------------|
| chalk | 터미널 색상 | 입력 검증 불필요 |
| commander | CLI 파싱 | 명령어 주입 방지됨 |
| conf | 설정 저장 | 로컬 파일 암호화 미지원 |
| inquirer | 대화형 입력 | 입력값 검증됨 |
| fs (Node.js) | 파일 시스템 | 경로 검증 필요 |

### 취약점 대응

1. **자동 스캔**: `npm audit` 정기 실행
2. **긴급 패치**: 중요 취약점 발견 시 24시간 내 패치
3. **의존성 고정**: `package-lock.json` 사용

---

## 보안 체크리스트

### 배포 전 확인

- [x] `npm audit` 취약점 0개
- [x] 하드코딩된 비밀 정보 없음
- [x] 로그에 민감 정보 미포함
- [x] 파일 경로 검증됨
- [x] 토큰 마스킹 동작 확인

### 코드 보안

- [x] 사용자 입력 검증
- [x] 에러 메시지에 상세 정보 미노출
- [x] TypeScript strict 모드 활성화
- [x] ESM 모듈 시스템 사용

---

## 알려진 제한사항

| 항목 | 설명 | 대응 |
|------|------|------|
| 로컬 저장 | 로그 파일 암호화 미지원 | 파일 권한으로 보호 |
| 단일 사용자 | 다중 사용자 환경 미고려 | 향후 개선 예정 |
| 감사 로그 | 접근 감사 로그 없음 | v0.3.0에서 추가 예정 |

---

## 보안 업데이트 이력

| 날짜 | 버전 | 내용 |
|------|------|------|
| 2026-02-01 | 0.1.0 | 초기 릴리즈, 보안 정책 수립 |

---

## 연락처

- **보안 이메일**: security@example.com
- **GitHub Issues**: 일반적인 버그 리포트용
- **GitHub Security**: 비공개 취약점 신고용

---

**마지막 업데이트**: 2026-02-01
